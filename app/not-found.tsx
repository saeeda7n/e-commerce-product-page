import React from "react";

const NotFound = () => {
 return (
  <div className="flex min-h-[calc(100vh-theme(spacing.20))] flex-1 items-center justify-center">
   Page not found!
  </div>
 );
};

export default NotFound;
